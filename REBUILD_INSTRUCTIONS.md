# Инструкция по пересборке Docker контейнеров

## Проблема
Docker Desktop запущен, но daemon еще не готов к работе.

## Решение

### Вариант 1: Использовать готовый скрипт

```powershell
.\rebuild-containers.ps1
```

Скрипт автоматически:
- Проверит доступность Docker
- Остановит существующие контейнеры
- Пересоберет backend и frontend без кэша
- Запустит контейнеры
- Покажет статус

### Вариант 2: Ручная пересборка

1. **Убедитесь, что Docker Desktop полностью запущен**
   - Иконка в трее должна быть зеленой
   - Проверьте: `docker ps` должен работать без ошибок

2. **Остановите существующие контейнеры:**
   ```powershell
   cd e:\HttpServer
   docker-compose down
   ```

3. **Пересоберите контейнеры без кэша:**
   ```powershell
   docker-compose build --no-cache
   ```

4. **Запустите контейнеры:**
   ```powershell
   docker-compose up -d
   ```

5. **Проверьте статус:**
   ```powershell
   docker-compose ps
   ```

### Вариант 3: Пересборка отдельных сервисов

Если нужно пересобрать только один сервис:

**Backend:**
```powershell
docker-compose build --no-cache backend
docker-compose up -d backend
```

**Frontend:**
```powershell
docker-compose build --no-cache frontend
docker-compose up -d frontend
```

## Что включено в пересборку

### Backend контейнер включает:
- ✅ Word-based duplicate detection функциональность
- ✅ Обновленный API endpoint `/api/quality/duplicates` с полными данными элементов
- ✅ Все последние изменения в Go коде

### Frontend контейнер включает:
- ✅ Поддержку отображения `word_based` дубликатов
- ✅ Зеленый бейдж "Группировка по словам" 🟢
- ✅ Отображение причины группировки (общие слова)
- ✅ Обновленные интерфейсы TypeScript

## Проверка после пересборки

1. **Backend должен быть доступен:**
   ```powershell
   curl http://localhost:9999/health
   ```

2. **Frontend должен быть доступен:**
   - Откройте браузер: http://localhost:3000
   - Перейдите на страницу дубликатов: http://localhost:3000/quality/duplicates

3. **Проверьте логи:**
   ```powershell
   docker-compose logs backend
   docker-compose logs frontend
   ```

## Если Docker все еще не работает

1. **Перезапустите Docker Desktop:**
   - Закройте Docker Desktop полностью
   - Запустите снова
   - Дождитесь полной загрузки (иконка в трее станет зеленой)

2. **Проверьте, что WSL2 работает (если используется):**
   ```powershell
   wsl --list --verbose
   ```

3. **Альтернатива - запуск без Docker:**
   - Backend: `go run main_no_gui.go`
   - Frontend: `cd app && npm run dev` (или `cd frontend && npm run dev`)

## Порты

- **Backend**: http://localhost:9999
- **Frontend**: http://localhost:3000

## Переменные окружения

Backend использует переменные из `docker-compose.yml`:
- `ARLIAI_API_KEY` - ключ API для AI классификации
- `ARLIAI_MODEL` - модель AI (по умолчанию: GLM-4.5-Air)

Frontend использует:
- `BACKEND_URL=http://backend:9999` (внутри Docker сети)
- Для локального запуска: `BACKEND_URL=http://localhost:9999`

