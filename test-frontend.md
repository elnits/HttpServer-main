# Тестирование Frontend

## Чек-лист для ручного тестирования

### 1. Главная страница (/)
- [ ] Страница загружается без ошибок
- [ ] Отображается статистика БД
- [ ] Отображается статистика нормализации
- [ ] Все ссылки работают
- [ ] Индикатор БД показывает "Сервисная БД"

### 2. Страница клиентов (/clients)
- [ ] Страница загружается
- [ ] Отображается список клиентов (или пустое состояние)
- [ ] Кнопка "Новый клиент" работает
- [ ] Поиск работает (если есть)
- [ ] Карточки клиентов отображаются корректно

### 3. Создание клиента (/clients/new)
- [ ] Форма отображается
- [ ] Все поля доступны для ввода
- [ ] Валидация работает
- [ ] Кнопка "Создать клиента" работает
- [ ] После создания происходит редирект
- [ ] Ошибки отображаются корректно

### 4. Профиль клиента (/clients/[id])
- [ ] Информация о клиенте отображается
- [ ] Статистика отображается
- [ ] Список проектов отображается
- [ ] Кнопка "Новый проект" работает
- [ ] Ссылки на проекты работают

### 5. Проекты клиента (/clients/[id]/projects)
- [ ] Список проектов отображается
- [ ] Кнопка создания проекта работает
- [ ] Карточки проектов отображаются
- [ ] Фильтры работают (если есть)

### 6. Создание проекта (/clients/[id]/projects/new)
- [ ] Форма отображается
- [ ] Все поля доступны
- [ ] Валидация работает
- [ ] Создание проекта работает
- [ ] Редирект после создания

### 7. Детали проекта (/clients/[id]/projects/[id])
- [ ] Информация о проекте отображается
- [ ] Статистика эталонов отображается
- [ ] Кнопка "Запустить нормализацию" работает
- [ ] Ссылка на эталоны работает

### 8. Эталоны проекта (/clients/[id]/projects/[id]/benchmarks)
- [ ] Список эталонов отображается
- [ ] Поиск работает
- [ ] Фильтры работают
- [ ] Таблица отображается корректно

### 9. Нормализация для клиента (/clients/[id]/projects/[id]/normalization)
- [ ] Страница загружается
- [ ] Статистика отображается
- [ ] Кнопка "Запустить нормализацию" работает
- [ ] Статус обновляется в реальном времени

### 10. Нормализация (/normalization)
- [ ] Страница загружается
- [ ] Статус отображается
- [ ] Кнопки управления работают
- [ ] Логи отображаются
- [ ] Статистика обновляется

### 11. Результаты (/results)
- [ ] Список групп отображается
- [ ] Фильтры работают
- [ ] Пагинация работает
- [ ] Детали группы отображаются

### 12. Базы данных (/databases)
- [ ] Список БД отображается
- [ ] Текущая БД отображается
- [ ] Переключение БД работает

### 13. Навигация
- [ ] Все ссылки в header работают
- [ ] Мобильная навигация работает
- [ ] Индикатор БД в header работает
- [ ] Переключение темы работает (если есть)

### 14. Обработка ошибок
- [ ] Ошибки API отображаются пользователю
- [ ] Сообщения об ошибках понятны
- [ ] Приложение не падает при ошибках
- [ ] Дефолтные значения отображаются при недоступности backend

## Автоматические проверки

Запустите в консоли браузера (F12):

```javascript
// Проверка всех API endpoints
const endpoints = [
  '/api/clients',
  '/api/database/info',
  '/api/normalization/status',
  '/api/databases/list'
];

endpoints.forEach(async (endpoint) => {
  try {
    const response = await fetch(endpoint);
    console.log(`${endpoint}: ${response.status} ${response.ok ? '✓' : '✗'}`);
  } catch (error) {
    console.error(`${endpoint}: ERROR - ${error.message}`);
  }
});
```

