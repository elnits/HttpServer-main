# HTTP Status Checker

Автоматизированный инструмент для проверки HTTP-статусов веб-страниц и API эндпоинтов.

## Возможности

✅ **Массовая проверка** - проверка множества URL одновременно  
✅ **Обработка таймаутов** - настраиваемые таймауты для каждого запроса  
✅ **Повторные попытки** - автоматические повторы при ошибках  
✅ **Логирование** - детальные логи с временными метками  
✅ **Отчеты** - JSON отчеты с полной статистикой  
✅ **Валидация заголовков** - проверка обязательных заголовков ответа  
✅ **Категоризация** - группировка URL по категориям  
✅ **Ожидаемые статусы** - проверка соответствия ожидаемым кодам  
✅ **Мониторинг** - интеграция с системами мониторинга через exit codes  

## Быстрый старт

### 1. Компиляция

```powershell
go build -o http_checker.exe ./cmd/http_checker/main.go
```

### 2. Использование PowerShell скрипта

```powershell
# Базовое использование
.\http_check.ps1

# С указанием конфигурации
.\http_check.ps1 -Config http_check_config.json

# С файлом URL
.\http_check.ps1 -Urls urls_example.txt

# С кастомными параметрами
.\http_check.ps1 -Timeout 10 -Retries 5 -Concurrent 10
```

### 3. Прямое использование

```powershell
# Проверка одного URL
.\http_checker.exe http://localhost:9999/health

# Проверка нескольких URL
.\http_checker.exe http://localhost:9999/health http://localhost:3000

# С конфигурацией
.\http_checker.exe -config http_check_config.json -output report.json

# С файлом URL
.\http_checker.exe -urls urls_example.txt -output report.json -log logs/check.log
```

## Конфигурация

### JSON конфигурация

Создайте файл `http_check_config.json`:

```json
{
  "timeout_seconds": 7,
  "max_retries": 3,
  "retry_delay_seconds": 1,
  "concurrent_checks": 5,
  "user_agent": "HTTP-Checker/1.0",
  "follow_redirects": true,
  "max_redirects": 5,
  "headers": {
    "Accept": "text/html,application/xhtml+xml"
  },
  "urls": [
    {
      "url": "http://localhost:9999/health",
      "method": "GET",
      "expected_status": 200,
      "category": "critical",
      "required_headers": {
        "Content-Type": "application/json"
      }
    }
  ]
}
```

### Параметры конфигурации

- `timeout_seconds` - таймаут для запросов (по умолчанию 7 секунд)
- `max_retries` - максимальное количество повторов (по умолчанию 3)
- `retry_delay_seconds` - задержка между повторами (по умолчанию 1 секунда)
- `concurrent_checks` - количество одновременных проверок (по умолчанию 5)
- `user_agent` - User-Agent для запросов
- `follow_redirects` - следовать ли редиректам (по умолчанию true)
- `max_redirects` - максимальное количество редиректов (по умолчанию 5)
- `headers` - дополнительные заголовки для всех запросов
- `urls` - массив URL для проверки

### Параметры URL

- `url` - URL для проверки (обязательно)
- `method` - HTTP метод (по умолчанию GET)
- `expected_status` - ожидаемый HTTP статус (опционально)
- `category` - категория URL (для группировки в отчетах)
- `required_headers` - обязательные заголовки ответа
- `timeout_seconds` - индивидуальный таймаут для URL

## Файл со списком URL

Создайте текстовый файл с URL (по одному на строку):

```
http://localhost:9999/health
http://localhost:9999/api/status
http://localhost:3000
```

Строки, начинающиеся с `#`, игнорируются как комментарии.

## Отчеты

### JSON отчет

Отчет сохраняется в формате JSON со следующей структурой:

```json
{
  "start_time": "2025-11-16T19:30:00Z",
  "end_time": "2025-11-16T19:30:05Z",
  "duration_seconds": 5.123,
  "total_checks": 10,
  "summary": {
    "success": 8,
    "client_errors": 1,
    "server_errors": 0,
    "redirects": 0,
    "timeouts": 1,
    "invalid": 0,
    "total_errors": 2
  },
  "results": [
    {
      "url": "http://localhost:9999/health",
      "status": 200,
      "status_text": "200 OK",
      "response_time_ms": 45.2,
      "headers": {
        "Content-Type": "application/json"
      },
      "attempts": 1,
      "timestamp": "2025-11-16T19:30:01Z",
      "category": "critical",
      "expected_status": 200,
      "is_valid": true
    }
  ]
}
```

### Интерпретация статусов

- **200-299** - Успешные ответы ✅
- **300-399** - Редиректы 🔄
- **400-499** - Клиентские ошибки ⚠️
- **500-599** - Серверные ошибки 🔴
- **0 или ошибка** - Таймауты/Ошибки подключения ⏱️

## Логирование

Логи сохраняются в директории `logs/` с именем файла:
```
http_check_YYYYMMDD_HHMMSS.log
```

Или укажите свой путь:
```powershell
.\http_checker.exe -log custom.log
```

## Интеграция с мониторингом

### Exit Codes

- `0` - Все проверки успешны
- `1` - Обнаружены критические ошибки (серверные ошибки, таймауты, недоступные критичные URL)

### Уведомления

HTTP Checker поддерживает отправку уведомлений через:

1. **Telegram** - через переменные окружения:
   ```powershell
   $env:TELEGRAM_BOT_TOKEN = "your_bot_token"
   $env:TELEGRAM_CHAT_ID = "your_chat_id"
   ```

2. **Webhook** - через переменную окружения:
   ```powershell
   $env:WEBHOOK_URL = "https://your-webhook-url.com/notify"
   ```

Уведомления отправляются автоматически при обнаружении ошибок уровня `error` или выше (настраивается).

### Пример использования в cron/Task Scheduler

```powershell
# Task Scheduler (Windows)
.\http_check.ps1 -Config http_check_config.json -Output reports\check.json
if ($LASTEXITCODE -ne 0) {
    # Отправка уведомления
    Send-MailMessage -To "admin@example.com" -Subject "HTTP Check Failed" -Body "Critical errors detected"
}
```

### Пример интеграции с системой мониторинга

```powershell
# Проверка и отправка уведомлений
$report = Get-Content reports\http_check_*.json | ConvertFrom-Json

if ($report.summary.total_errors -gt 0) {
    # Отправка в Telegram (пример)
    $token = "YOUR_BOT_TOKEN"
    $chatId = "YOUR_CHAT_ID"
    $message = "🔴 Обнаружено $($report.summary.total_errors) ошибок при проверке HTTP статусов"
    
    Invoke-RestMethod -Uri "https://api.telegram.org/bot$token/sendMessage" `
        -Method Post `
        -Body @{chat_id=$chatId; text=$message}
}
```

## Примеры использования

### Проверка API эндпоинтов

```json
{
  "urls": [
    {
      "url": "http://localhost:9999/api/normalize/status",
      "method": "GET",
      "expected_status": 200,
      "category": "api",
      "required_headers": {
        "Content-Type": "application/json"
      }
    }
  ]
}
```

### Проверка с разными методами

```json
{
  "urls": [
    {
      "url": "http://localhost:9999/api/data",
      "method": "POST",
      "expected_status": 201,
      "category": "api"
    }
  ]
}
```

### Проверка с индивидуальными таймаутами

```json
{
  "urls": [
    {
      "url": "http://slow-api.example.com/status",
      "method": "GET",
      "expected_status": 200,
      "category": "external",
      "timeout_seconds": 30
    }
  ]
}
```

## Автоматизация

### Настройка Task Scheduler (Windows)

```powershell
# Запустите от имени администратора
.\setup_http_check_scheduler.ps1 -IntervalMinutes 15
```

Это создаст задачу, которая будет запускать проверки каждые 15 минут.

### Ручной запуск по расписанию

```powershell
.\http_check_scheduled.ps1
```

Скрипт автоматически:
- Создает отчеты с временными метками
- Анализирует результаты
- Сохраняет отчеты об ошибках
- Очищает старые отчеты (старше 30 дней)

## Рекомендации

1. **Критичные URL** - помечайте важные URL категорией `"critical"` для особого внимания
2. **Таймауты** - устанавливайте разумные таймауты в зависимости от типа ресурса
3. **Повторы** - используйте повторы для нестабильных соединений
4. **Мониторинг** - настройте автоматические проверки через Task Scheduler или cron
5. **Логи** - регулярно проверяйте логи для выявления паттернов ошибок
6. **Уведомления** - настройте Telegram или Webhook для получения уведомлений об ошибках

## Устранение проблем

### Таймауты

Если часто возникают таймауты:
- Увеличьте `timeout_seconds` в конфигурации
- Установите индивидуальные таймауты для медленных URL
- Проверьте сетевое соединение

### Ошибки 429 (Too Many Requests)

- Уменьшите `concurrent_checks`
- Увеличьте `retry_delay_seconds`
- Используйте rate limiting на стороне сервера

### Ложные срабатывания

- Проверьте правильность `expected_status`
- Убедитесь, что `required_headers` указаны корректно
- Проверьте логи для деталей

